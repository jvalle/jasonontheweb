extends layout

block content
    .content
        p coming soon chumps
        ul#articles
            - each article in articles
                li.article
                    h2.title
                        a(href="/blog/"+article.postUrl)!= article.title
                    .created-at posted on #{ article.display_date }
                    .article-body!= article.body